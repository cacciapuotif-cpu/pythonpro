// Colori allineati al gestionale web
const tintColorLight = '#3174ad'; // Blu primario del gestionale
const tintColorDark = '#fff';

export default {
  light: {
    text: '#333', // Testo principale (allineato al gestionale)
    background: '#f5f7fa', // Background (allineato al gestionale)
    tint: tintColorLight,
    tabIconDefault: '#ccc',
    tabIconSelected: tintColorLight,
  },
  dark: {
    text: '#fff',
    background: '#000',
    tint: tintColorDark,
    tabIconDefault: '#ccc',
    tabIconSelected: tintColorDark,
  },
};
